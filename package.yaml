name:                blog
version:             0.1.0.0
github:              "schnecki/schnecki.github.io"
license:             AllRightsReserved
author:              "Manuel Schneckenreither"
maintainer:          "manuel.schneckenreither@uibk.ac.at"
copyright:           "2020 Manuel Schneckenreither"

extra-source-files:
- README.md

# Metadata used when publishing your package
# synopsis:            Short description of your package
# category:            Web

# To avoid duplicated efforts in documentation and dealing with the
# complications of embedding Haddock markup inside cabal files, it is
# common to point users to the README.md file.
description:         Please see the README on GitHub at <https://github.com/schnecki/schnecki.github.io#readme>

dependencies:
  - base >= 4.7 && < 5
  - hakyll

# library:
#   source-dirs: src
#   ghc-options:
#     -Wall
#     -Wcompat
#     -Wincomplete-record-updates
#     -Wincomplete-uni-patterns
#     -Wredundant-constraints
#     -fwarn-tabs
#     -fno-warn-name-shadowing
#     -O2
#     -funbox-strict-fields

executables:
  blog:
    main:                site.hs
    ghc-options:
    - -threaded
    - -rtsopts
    - -with-rtsopts=-N
    - -O2
    - -funbox-strict-fields

    dependencies:
    - base >= 4.7 && < 5
    - hakyll

